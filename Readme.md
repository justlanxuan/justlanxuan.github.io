## A workflow
1. build the web, update posts in posts/*.md, where * is the date today.
```bash
npm run build
```
2. update on git
```bash
git add .
git commit -m"update"
git push
```
3. For development
```bash
npm run dev
```
